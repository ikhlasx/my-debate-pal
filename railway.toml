[build]
builder = "nixpacks"

[deploy]
startCommand = ".venv/bin/uvicorn backend.main_supabase:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
